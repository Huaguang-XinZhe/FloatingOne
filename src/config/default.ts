import { FloatOneConfig } from "@/types";

export const DEFAULT_CONFIG: FloatOneConfig = {
  tips: [
    "先持续，后加量！\n> 至少一个月，到 8-26",
    "必须挣钱！不只是生存！",
    "多行提示示例\n第二行内容\n> 这是补充描述",
  ],
  autoRotate: true,
  rotateInterval: 60,
  theme: "dark",
  autoStart: true,
};
